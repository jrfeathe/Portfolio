# Resume JSON/Print (Archived)

This folder holds the old `/resume.json` endpoint, printable resume page, and related tests. The site now ships only the PDF download; these files are parked here to keep history without shipping the routes in production.
